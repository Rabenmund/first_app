<% if object.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-error">
      Schade, das Formular enthält leider <%= object.errors.count%> Fehler. Schau mal:
    </div>
    <ul>
    <% object.errors.full_messages.each do |msg| %>
      <li>* <%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>