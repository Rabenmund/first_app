<% @user ||= current_user %>
<div class="stats">
  <a href="<%= user_path(@user) %>">
	Gewinn
    <strong id="account" class="stat">
      <%= "< 25 Euro >" %>
    </strong>
  </a>
  <a href="<%= user_path(@user) %>">
    Nachricht
 	<strong id="message" class="stat">
      <%= "< Foto fehlt! >" %>
    </strong>
  </a>
</div>